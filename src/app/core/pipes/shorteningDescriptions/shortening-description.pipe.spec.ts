import { ShorteningDescriptionPipe } from './shortening-description.pipe';

describe('ShorteningDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShorteningDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
